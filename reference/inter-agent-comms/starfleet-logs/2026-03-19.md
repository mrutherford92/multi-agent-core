# Starfleet Log — 2026-03-19

## 🔵 Bridge — Session Report

**Session Time**: 2026-03-19

### Features Shipped

#### 1. Profile Action Buttons Overhaul
- **5 buttons**: Follow, Message, Connection, Invite to Circle, Invite to Group
- All buttons 25% larger (45×45px), fit on one row
- Replaced chat toolbar icon with AI Sparkle (✨)

#### 2. Group Invite → Notification Workflow
- Previously: `_addToGroup` directly called `GroupService.joinGroup()` (no notification, silent add)
- Now: Sends a `groupInvite` notification via `NotificationsService.sendNotification()`
- Target user sees Accept/Decline buttons in Notifications screen
- Accept calls `GroupService.joinGroup()`, Decline dismisses
- Added `groupInvite` to `NotificationType` enum
- Added `groupInvites` toggle to `NotificationPreferences` model
- Added toggle to Notification Settings screen

#### 3. AI Commonalities — Markdown Rendering
- Fixed raw markdown display in the AI analysis bottom sheet
- Replaced `SelectableText` with `MarkdownBody` from `flutter_markdown_plus`
- Added `MarkdownStyleSheet` matching chatbot screen dark theme

#### 4. Profile Stats Row — Chip Buttons
- Centered the stats+badges row
- Redesigned individual stats as tappable pill-shaped chips
- Added rounded borders, subtle background tint, vertically centered chevrons
- Adjusted font sizes: count 15px, label 14px, chevron 16px, badges 22px

### Files Modified
- `lib/features/user_profile/views/public_profile_screen.dart` — action buttons, AI sparkle, group invite workflow, markdown rendering, stats UI
- `lib/features/notifications/models/notification_model.dart` — `groupInvite` enum
- `lib/features/notifications/models/notification_preferences_model.dart` — `groupInvites` field
- `lib/features/notifications/services/notifications_service.dart` — `groupInvite` handling
- `lib/features/notifications/views/notifications_screen.dart` — Accept/Decline UI
- `lib/features/notifications/views/notification_settings_screen.dart` — Group Invites toggle

### Docs Updated
- `docs/qa/MANUAL_TEST_CASES.md` — Pruned outdated Manage Circles section, updated Group Invite tests for notification workflow, added AI Commonalities and Stats Row test sections

### Build
- Ready to ship via `scripts/mongo_build.sh` (dual-platform: Android + iOS)
